#include "main.h"

/**
 * _strlen - returns lenght of a string
 * @s: string
 * Returns: returns the length as integer;
 */

int_strlen(char *s)
{
	int len = 0;
	
	while (*(s = len) != '\0')
		len++;
}
